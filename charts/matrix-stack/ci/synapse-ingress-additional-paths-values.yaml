# Copyright 2024-2025 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial
#
# source_fragments: synapse-minimal.yaml synapse-test-postgres.yaml synapse-ingress-additional-paths.yaml init-secrets-minimal.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

initSecrets:
  enabled: true
elementWeb:
  enabled: false
matrixAuthenticationService:
  enabled: false
synapse:
  ingress:
    host: synapse.ess.localhost
    additionalPaths:
      - path: /_matrix/identity
        availability: only_externally
        service:
          name: sydent
          port:
            number: 8080
      - path: /_synapse
        availability: blocked
      - path: /other
        availability: internally_and_externally
        service:
          name: something
          port:
            name: http
  postgres:
    host: ess-postgres
    user: synapse_user
    database: synapse
    password:
      secret: ess-postgres
      secretKey: password
wellKnownDelegation:
  enabled: false
serverName: ess.localhost
