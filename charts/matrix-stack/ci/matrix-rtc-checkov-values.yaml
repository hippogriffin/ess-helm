# Copyright 2024-2025 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only
#
# source_fragments: matrix-rtc-minimal.yaml matrix-rtc-checkov.yaml init-secrets-checkov.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# postgres don't have any required properties to be set and defaults to enabled
elementWeb:
  enabled: false
initSecrets:
  annotations:
    checkov.io/skip1: CKV_K8S_38=The job needs a service account
matrixAuthenticationService:
  enabled: false
matrixRTC:
  ingress:
    host: mrtc.ess.localhost
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
