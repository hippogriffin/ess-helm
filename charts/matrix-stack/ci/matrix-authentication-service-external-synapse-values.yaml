# Copyright 2024-2025 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial
#
# source_fragments: matrix-authentication-service-minimal.yaml matrix-authentication-service-test-postgres.yaml matrix-authentication-service-external-synapse.yaml init-secrets-minimal.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

initSecrets:
  enabled: true
elementWeb:
  enabled: false
matrixAuthenticationService:
  ingress:
    host: mas.ess.localhost
  postgres:
    host: postgres
    user: mas
    database: mas
    password:
      secret: ess-postgres
      secretKey: password
  additional:
    0000-matrix-server:
      config: |
        clients:
        - client_id: "0000000000000000000SYNAPSE"
          client_auth_method: client_secret_basic
          client_secret: CHANGEME-eiv6wae8shooPhie4ief8ru2egahbah0
        matrix:
          homeserver: "external.localhost
          secret: jaix6Am9Shut7zeiduu7ua5maengag3o
          endpoint: https://syn.external.localhost
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
