# Copyright 2024-2025 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial
#
# source_fragments: element-web-minimal.yaml synapse-minimal.yaml matrix-authentication-service-minimal.yaml init-secrets-minimal.yaml well-known-minimal.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

initSecrets:
  enabled: true
elementWeb:
  ingress:
    host: element.ess.localhost
matrixAuthenticationService:
  ingress:
    host: mas.ess.localhost
postgres:
  enabled: false
synapse:
  ingress:
    host: synapse.ess.localhost
wellKnownDelegation:
  enabled: true
serverName: ess.localhost
