# Copyright 2024-2025 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial
#
# source_fragments: synapse-minimal.yaml synapse-pytest-extras.yaml matrix-authentication-service-minimal.yaml matrix-authentication-service-pytest-extras.yaml init-secrets-minimal.yaml init-secrets-pytest-extras.yaml postgresql-minimal.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

initSecrets:
  enabled: true
  annotations:
    has-no-service-monitor: "true"
elementWeb:
  enabled: false
matrixAuthenticationService:
  ingress:
    host: mas.{{ $.Values.serverName }}
    tlsSecret: "{{ $.Release.Name }}-mas-web-tls"
  extraEnv:
    - name: DEBUG_RENDERING
      value: "1"
  additional:
    000-pytest-admin:
      configSecret: "{{ $.Release.Name }}-pytest-admin"
      configSecretKey: "admin.yaml"
postgresql:
  enabled: true
synapse:
  ingress:
    host: synapse.{{ $.Values.serverName }}
    tlsSecret: "{{ $.Release.Name }}-synapse-web-tls"
  additional:
    00-userconfig.yaml:
      config: |
        push:
          jitter_dalay: 10
    01-other-user-config.yaml:
      configSecret: "{{ $.Release.Name }}-synapse-secrets"
      configSecretKey: "01-other-user-config.yaml"
  extraEnv:
    - name: DEBUG_RENDERING
      value: "1"
  redis:
    annotations:
      has-no-service-monitor: "true"
  workers:
    sliding-sync:
      enabled: true
    media-repository:
      enabled: true
wellKnownDelegation:
  enabled: false
serverName: ess.localhost
