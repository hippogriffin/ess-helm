# Copyright 2024 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only OR LicenseRef-Element-Commercial

fullnameOverride: ess-postgres

auth:
  database: synapse
  username: synapse_user

primary:
  # Can be overridden by specifying POSTGRES_RESOURCES_PRESET when calling scripts/setup_test_cluster.sh
  resourcesPreset: micro

  initdb:
    args: --locale=C --encoding=UTF8
